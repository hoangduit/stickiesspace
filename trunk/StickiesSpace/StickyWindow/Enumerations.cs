

    //What state the textbox is in (editable vs. dragable)
    public enum TextBoxActiveState
    {
        Active, Inactive
    }

    //When abstracting property binding are we setting or clearing in this call
    public enum SetBindingMode
    {
        SetBinding,
        ClearBinding
    }

    //When abstracting slider animation routines are we showing or hiding in this call
    public enum SliderAnimateMode
    {
        Show, Hide
    }

